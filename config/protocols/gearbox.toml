# Gearbox V3 mainnet (not in registry.json).
# Fills `liq_adapters_gearbox::Config` after live `getCreditManagers()` +
# per-manager `fees()`. Never a hand list of 34 managers.
#
# Pin: Gearbox-protocol/core-v3 @ 510fc6541c3767ce825929b4c311826fe81d6fa5
# Register D15: 0xA50d4E7D8946a7c90652339CDBd262c375d54D99 (cardinality 34).
# Intern: ProtocolId 11; catalog MarketId 4200; managers 4201..=4299.
# Underlyings are on-chain. Do not invent a USDC/WETH list.
# FeedId(0) is the first interned Aave oracle, not unset. Gearbox oracles are
# not joined via row.price_feed; health prices via AssetId / PriceVector.

protocol = 11
register = "0xA50d4E7D8946a7c90652339CDBd262c375d54D99"
catalog = 4200
first_market = 4201
expected_managers = 34
pinned_through = 26014442
