# GUIDE 16 §5 — feed vs submission queue map.
# iface=CHANGE_ME keeps verify from PASSing until an operator applies queues.sh
# on the Linux box and writes state.applied. Do not invent a NIC name.

iface=CHANGE_ME
submit_tx_queue=0
feed_tx_queue=1
